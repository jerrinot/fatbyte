{
  "name": "jar-bytecode-analyzer",
  "version": "1.0.0",
  "description": "A single-page static web application that analyzes JAR files and displays the largest methods by bytecode size",
  "type": "module",
  "scripts": {
    "generate-fixtures": "cd test && bash generate-fixtures.sh",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "jszip": "^3.10.1",
    "vitest": "^2.1.8"
  }
}
